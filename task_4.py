a = ['dd ere rere', 'sdsd']

for el in a:
    el = list(el)
    len(el) #2
    name = a[len(a) - 1]
    # counter = 0
    # for letter in name:
    #     counter += 1
    #     if counter != 1:
    #         letter
    print(f"Привет, {name}")
